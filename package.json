{
  "name": "postman-project",
  "version": "1.0.0",
  "description": "Demo Project for Postman-Newman-Jenkins Integration",
  "directories":{
    "tests": "tests"
  },
  "scripts": {
    "api-tests-production": "newman run tests/json-placeholder-project.postman_collection.json --reporters junit --reporter-junit-export newman.xml"
  },
  "author": "Jeffrey Encarnacion",
  "dependencies": {
    "newman": "^3.9.3"
  }
}
